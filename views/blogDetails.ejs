<!DOCTYPE html>
<html lang="en">
    <%- include('partials/head') %>
<body>
    <%- include('partials/nav') %>
    <div class="container mt-5">
        <div class="card shadow-lg p-3">
            <img src="/<%= blog.image_url %>" class="card-img-top img-fluid" alt="Blog Image" style="max-height: 400px; object-fit: cover;">
            <div class="card-body">
                <h1 class="card-title text-center"><%= blog.title %></h1>
                <div class="card-text" style="max-height: 300px; overflow-y: auto;">
                    <%= blog.content %>
                </div>
            </div>
        </div>
    </div>
</body>
</html>